# 📊 Дашборд бенчарков Вопрошалыча

Интерактивный интерфейс для просмотра метрик качества RAG-быстрого поиска.

## Запуск

```bash
# Из директории Submodules/voproshalych/benchmarks
python run_dashboard.py

# Из родительской директории проекта
python Submodules/voproshalych/benchmarks/run_dashboard.py
```

Дашборд будет доступен по адресу: http://localhost:8501

## Функционал

- 📈 **Обзор метрик** - сравнение разных бенчарков
- 📊 **История изменений** - графики метрик во времени
- 🔍 **Фильтрация** - по датасетам, типам бенчарков, датам
- 📊 **Сравнение** - side-by-side сравнение двух запусков

## Структура отчётов

Отчёты бенчарков хранятся в `benchmarks/reports/`:

```
benchmarks/reports/
├── retrieval_tier1_golden_set_20260213_*.md      # Отчёты Markdown
├── retrieval_tier1_golden_set_20260213_*.json      # Метрики JSON
├── retrieval_tier1_low_quality_20260213_*.md
├── retrieval_tier2_golden_set_20260213_*.md
└── ...
```

## Метрики

| Метрика | Описание | Отлично | Хорошо | Средне | Плохо |
|---------|----------|---------|--------|--------|--------|
| **Tier 1** | | | | | |
| MRR | Обратный ранг первого релевантного | ≥0.8 | ≥0.6 | ≥0.4 | <0.4 |
| Recall@1 | Доля на первой позиции | ≥80% | ≥60% | ≥40% | <40% |
| Precision@5 | Точность топ-5 | ≥80% | ≥60% | ≥40% | <40% |
| | **Tier 2** | | | | | |
| MRR | Обратный ранг первого релевантного | ≥0.5 | ≥0.4 | ≥0.3 | <0.3 |
| Recall@10 | Доля в топ-10 | ≥70% | ≥50% | ≥30% | <30% |
| Precision@5 | Точность топ-5 | ≥50% | ≥40% | ≥30% | <30% |

## Технические требования

- Python 3.11+
- streamlit
- gradio (для виджетов)
- matplotlib/plotly (для графиков)

## Автозагрузка дампа

Дашборд поддерживает автоматическую загрузку дампа БД через кнопку **"Проверить статистику"**.

### Формат дампа

- `.sql` - SQL дамп
- `.sql.gz` - сжатый SQL дамп
- `.tar` - архив с SQL файлами
- `.tar.gz` - сжатый архив

### Расположение

Дампы должны находиться в `Submodules/voproshalych/benchmarks/data/dump/`.
