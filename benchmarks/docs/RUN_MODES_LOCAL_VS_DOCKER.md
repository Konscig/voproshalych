# Режимы работы модуля бенчмарков

Документ описывает два режима работы модуля бенчмарков RAG-системы Вопрошалыч: локальный и контейнерный.

---

## Локальный режим

### Цели

- Быстрое экспериментальное тестирование разработчиком
- Отладка пайплайна бенчмарков
- Итеративная разработка без Docker
- Минимум зависимостей

### Сервисы

Для локального режима достаточно:

| Сервис | Обязателен | Примечание |
|--------|------------|------------|
| PostgreSQL | ✅ Да | В контейнере или локально |
| Python скрипты | ✅ Да | Запуск через `uv run` |

**НЕ нужны:** qa, chatbot, adminpanel, max — код импортируется напрямую из модуля `qa/`.

### Хранение данных

| Тип данных | Где хранится |
|------------|--------------|
| Чанки и эмбеддинги | PostgreSQL (таблица `chunk`) |
| Вопросы пользователей | PostgreSQL (таблица `question_answer`) |
| История бенчмарков | PostgreSQL (таблица `benchmark_runs`) |
| Датасеты | Файлы `benchmarks/data/dataset_*.json` |
| Отчёты | Файлы `benchmarks/reports/*.json` и `*.md` |

### История запусков

История хранится в PostgreSQL:

```sql
SELECT * FROM benchmark_runs ORDER BY timestamp DESC;
```

Для переноса истории на другую машину:

```bash
# Экспорт дампа БД
pg_dump -h localhost -U postgres postgres > benchmark_history.dump

# Импорт на другой машине
psql -h localhost -U postgres postgres < benchmark_history.dump
```

### Быстрый старт

```bash
cd Submodules/voproshalych

# 1. Установка зависимостей
uv sync

# 2. Подготовка БД (если нужна)
uv run python benchmarks/load_database_dump.py --dump benchmarks/data/dump/virtassist_backup_20260213.dump

# 3. Генерация эмбеддингов
uv run python benchmarks/generate_embeddings.py --chunks

# 4. Генерация датасета
uv run python benchmarks/generate_dataset.py --max-questions 20

# 5. Запуск бенчмарков
uv run python benchmarks/run_comprehensive_benchmark.py --tier all --mode synthetic --limit 10

# 6. Дашборд
uv run python benchmarks/run_dashboard.py
```

---

## Контейнерный режим

### Цели

- Воспроизводимый CI/CD запуск
- Деплой на сервер/стенд
- Стабильная история бенчмарков
- Изолированное окружение

### Сервисы

Для контейнерного режима по умолчанию:

| Сервис | Обязателен | Примечание |
|--------|------------|------------|
| db | ✅ Да | PostgreSQL |
| benchmarks | ✅ Да | Контейнер с Python |
| db-migrate | ✅ Да | Миграции БД |
| qa | ❌ Нет | Только если нужен HTTP API |
| chatbot | ❌ Нет | Не нужен для бенчмарков |
| adminpanel | ❌ Нет | Не нужен для бенчмарков |
| max | ❌ Нет | Не нужен для бенчмарков |

**Важно:** Код `qa` импортируется внутрь контейнера benchmarks, поэтому HTTP доступ к qa не требуется.

### Профили Docker Compose

Для управления опциональными сервисами используются профили:

| Профиль | Запуск команды | Сервисы |
|---------|----------------|---------|
| (без профиля) | `docker compose -f docker-compose.benchmarks.yml up -d` | db, db-migrate, benchmarks |
| `api` | `--profile api up -d` | + qa, adminpanel |
| `bot` | `--profile bot up -d` | + chatbot |
| `full` | `--profile full up -d` | Все сервисы |

**Примеры:**

```bash
# Базовый стек (рекомендуется для бенчмарков)
docker compose -f docker-compose.benchmarks.yml up -d

# С qa и adminpanel (для HTTP API тестов)
docker compose -f docker-compose.benchmarks.yml --profile api up -d

# Полный стек
docker compose -f docker-compose.benchmarks.yml --profile full up -d
```

### Хранение данных

| Тип данных | Где хранится | Persistence |
|------------|--------------|-------------|
| База данных | Docker volume `db-data` | ✅ Персистентно |
| Кэш моделей | Docker volume `model-cache` | ✅ Персистентно |
| Отчёты | Docker volume `benchmarks-reports` | ✅ Персистентно |
| Датасеты | Docker volume `benchmarks-data` | ✅ Персистентно |

### История запусков

История хранится в PostgreSQL в Docker volume `db-data`.

Для переноса истории на другую машину:

```bash
# Вариант A: Экспорт volume
docker run --rm -v db-data:/data -v $(pwd):/backup alpine tar czf /backup/db-data.tar.gz /data

# Вариант B: Экспорт дампа через pg_dump
docker compose -f docker-compose.benchmarks.yml exec db pg_dump -U postgres postgres > benchmark_history.dump

# Импорт на другой машине
docker compose -f docker-compose.benchmarks.yml exec -T db psql -U postgres postgres < benchmark_history.dump
```

### Быстрый старт

```bash
cd Submodules/voproshalych

# 1. Поднять стек
docker compose -f docker-compose.benchmarks.yml up -d --build

# 2. Подготовка БД
docker compose -f docker-compose.benchmarks.yml exec -T benchmarks uv run python benchmarks/load_database_dump.py \
  --dump benchmarks/data/dump/virtassist_backup_20260213.dump

# 3. Генерация эмбеддингов
docker compose -f docker-compose.benchmarks.yml exec benchmarks uv run python benchmarks/generate_embeddings.py --chunks

# 4. Генерация датасета
docker compose -f docker-compose.benchmarks.yml exec benchmarks uv run python benchmarks/generate_dataset.py --max-questions 500

# 5. Запуск бенчмарков
docker compose -f docker-compose.benchmarks.yml exec benchmarks uv run python benchmarks/run_comprehensive_benchmark.py \
  --tier all --mode synthetic

# 6. Дашборд
docker compose -f docker-compose.benchmarks.yml run --rm -p 7860:7860 benchmarks uv run python benchmarks/run_dashboard.py
```

---

## Сравнение режимов

| Критерий | Локальный | Контейнерный |
|----------|-----------|--------------|
| Скорость запуска | ⚡ Быстро | 🐢 Медленнее |
| Воспроизводимость | ⚠️ Зависит от машины | ✅ Гарантирована |
| CI/CD | ❌ Сложно | ✅ Идеально |
| Отладка | ✅ Удобно | ⚠️ Сложнее |
| История | В БД | В БД + volumes |
| Перенос истории | pg_dump | pg_dump или volumes |

---

## Рекомендации по выбору режима

### Используйте локальный режим когда:

- Разрабатываете новые метрики
- Отлаживаете пайплайн
- Быстро проверяете гипотезы
- Работаете в одиночку

### Используйте контейнерный режим когда:

- Запускаете CI/CD пайплайн
- Деплоите на сервер
- Важна история бенчмарков
- Работаете в команде

---

## Связанные документы

- [SMOKE_SCENARIO_LOCAL.md](SMOKE_SCENARIO_LOCAL.md) — детальные инструкции для локального режима
- [SMOKE_SCENARIO_DOCKER.md](SMOKE_SCENARIO_DOCKER.md) — детальные инструкции для Docker режима
- [ARCHITECTURE.md](ARCHITECTURE.md) — архитектура модуля
