```sh
uv init -p 3.12    
uv venv --seed  

uv pip install git+https://github.com/salute-developers/GigaAM.git

uv pip install dotenv
uv pip install requests aiogram
uv pip install ffmpeg-python

# brew install wget
# brew install ffmpeg
```

