# Модуль для оценки качества RAG-системы Вопрошалыч

Комплексная система бенчмарков для оценки качества поиска и генерации ответов в чат-боте Вопрошалыч.

## Обзор

Система бенчмарков предназначена для объективной оценки качества RAG (Retrieval-Augmented Generation) архитектуры чат-бота. Она позволяет:

- 📊 Измерять метрики качества векторного поиска (recall, precision, MRR, NDCG)
- 📈 Отслеживать изменения метрик во времени
- 🔍 Сравнивать разные стратегии поиска
- 📋 Генерировать детальные отчёты с результатами
- 📉 Визуализировать метрики через интерактивный дашборд

## Структура проекта

```
benchmarks/
├── data/                         # Данные для бенчмарков
│   ├── raw/                      # Сырые данные из БД
│   ├── static/                   # Статические JSON-датасеты
│   └── dump/                     # Дампы базы данных
│
├── models/                       # Классы бенчарков
│   └── retrieval_benchmark.py   # Базовый класс бенчмарка
│
├── utils/                        # Утилиты и вспомогательные модули
│   ├── data_loader.py           # Загрузка тестовых данных
│   ├── embedding_generator.py  # Генерация эмбеддингов
│   ├── evaluator.py             # Вычисление метрик
│   ├── report_generator.py      # Генерация отчётов
│   ├── static_data_loader.py    # Загрузка статических датасетов
│   └── database_dump_loader.py  # Загрузка дампа БД
│
├── tests/                        # Тесты
│
├── reports/                      # Отчёты бенчмарков
│   ├── retrieval_tier1_*.md      # Отчёты Tier 1
│   ├── retrieval_tier2_*.md      # Отчёты Tier 2
│   └── *.json                    # Метрики в JSON
│
├── dashboard.py                  # Интерактивный дашборд (Gradio)
├── run_dashboard.py              # Скрипт запуска дашборда
├── generate_embeddings.py        # Генерация эмбеддингов
├── load_database_dump.py         # Загрузка дампа БД
├── run_benchmarks.py             # Запуск бенчмарков
├── DASHBORAD.md                  # Документация дашборда
└── README.md                     # Этот файл
```

## Компоненты системы

### 1. Генератор эмбеддингов

Генерирует векторные представления для вопросов и чанков с использованием модели multilingual-e5-large-wikiutmn.

**Запуск:**
```bash
# Генерация для всех вопросов
python benchmarks/generate_embeddings.py --all

# Генерация для вопросов с определённой оценкой
python benchmarks/generate_embeddings.py --score 5

# Проверка покрытия эмбеддингами
python benchmarks/generate_embeddings.py --check-coverage

# Перезапись существующих эмбеддингов
python benchmarks/generate_embeddings.py --all --overwrite
```

**Опции:**
| Опция | Описание |
|-------|----------|
| `--all` | Генерировать эмбеддинги для всех вопросов |
| `--score N` | Генерировать только для вопросов с оценкой N (1 или 5) |
| `--check-coverage` | Проверить покрытие эмбеддингами без генерации |
| `--overwrite` | Перезаписать существующие эмбеддинги |

### 2. Бенчмарки поиска

Оценивает качество векторного поиска на двух уровнях (Tier).

**Tier 1 — поиск похожих вопросов:**
- Поиск релевантных вопросов в базе
- Оценка на основе score и URL-совпадений

**Tier 2 — поиск чанков:**
- Поиск релевантных текстовых фрагментов (чанков)
- Оценка на основе принадлежности к релевантному вопросу

**Запуск:**
```bash
# Tier 1 с золотым набором
python benchmarks/run_benchmarks.py --tier 1 --dataset golden_set

# Tier 2 с вопросами по URL
python benchmarks/run_benchmarks.py --tier 2 --dataset questions_with_url

# Tier 1 с неавторизованными вопросами
python benchmarks/run_benchmarks.py --tier 1 --dataset low_quality

# Tier 2 с последними вопросами
python benchmarks/run_benchmarks.py --tier 2 --dataset recent --top-k 20
```

**Опции:**
| Опция | Описание | Значение по умолчанию |
|-------|----------|----------------------|
| `--tier N` | Уровень бенчмарка (1 или 2) | - |
| `--dataset DATASET` | Датасет для тестирования | - |
| `--top-k N` | Количество возвращаемых результатов | 10 |
| `--output-dir DIR` | Директория для отчётов | `benchmarks/reports` |

**Доступные датасеты:**
| Датасет | Описание | Размер |
|---------|----------|--------|
| `golden_set` | Вопросы с оценкой 5 (высокое качество) | ~400 |
| `low_quality` | Вопросы с оценкой 1 (низкое качество) | ~300 |
| `questions_with_url` | Вопросы с URL-ссылками | ~4600 |
| `recent` | Последние вопросы (6 месяцев) | ~1300 |

### 3. Загрузчик данных

Загружает тестовые данные из базы данных или статических JSON-файлов.

**Методы:**
- `load_golden_set()` — вопросы с score=5
- `load_low_quality_set(limit)` — вопросы с score=1
- `load_questions_with_url()` — вопросы с URL-ссылками
- `load_recent_questions(months, limit)` — последние N месяцев
- `prepare_ground_truth_urls(questions)` — подготовка ground truth

### 4. Оценщик метрик

Вычисляет классические метрики качества информационного поиска.

**Метрики:**
| Метрика | Формула | Интерпретация |
|---------|---------|--------------|
| `Recall@K` | Relevant_in_top_K / Total_relevant | Доля релевантных результатов в топ-K |
| `Precision@K` | Relevant_in_top_K / K | Точность среди топ-K результатов |
| `MRR` | Mean(1 / rank_of_first_relevant) | Средний обратный ранг первого релевантного результата |
| `NDCG@K` | DCG@K / IDCG@K | Нормализованный дисконтированный кумулятивный выигрыш |

### 5. Генератор отчётов

Генерирует детальные отчёты в формате Markdown и JSON.

**Формат отчёта:**
- Общая статистика
- Детальные метрики по каждому запросу
- Примеры успешных и неудачных поисков
- Анализ покрытия эмбеддингами

**Функции:**
- `generate_retrieval_report()` — генерация Markdown-отчёта
- `save_metrics_json()` — сохранение метрик в JSON
- `save_report()` — сохранение отчёта в файл

## Метрики качества

### Целевые значения

| Метрика | Tier 1 (вопросы) | Tier 2 (чанки) | Приоритет |
|---------|------------------|---------------|-----------|
| `Recall@1` | ≥ 0.80 | ≥ 0.50 | 🔴 Высокий |
| `Recall@5` | ≥ 0.90 | ≥ 0.70 | 🔴 Высокий |
| `Recall@10` | ≥ 0.95 | ≥ 0.80 | 🟡 Средний |
| `Precision@5` | ≥ 0.80 | ≥ 0.60 | 🟡 Средний |
| `MRR` | ≥ 0.80 | ≥ 0.55 | 🔴 Высокий |
| `NDCG@10` | ≥ 0.80 | ≥ 0.65 | 🟡 Средний |

### Покрытие эмбеддингами

| Метрика | Текущее | Цель | Статус |
|---------|---------|------|--------|
| QA с эмбеддингами | ~5% | ≥ 80% | ❌ Критично |
| Чанки с эмбеддингами | ~5% | ≥ 80% | ❌ Критично |

## Режимы работы

Система бенчмарков поддерживает два режима работы с различными сценариями использования.

### Режим 1: Статические датасеты (без Docker)

**Оптимально для:**
- Первого запуска и проверки работоспособности
- Разработки и отладки бенчарков
- Быстрого тестирования без развёртывания инфраструктуры
- Работы с изолированной средой

**Пайплайн:**
```bash
cd Submodules/voproshalych

# 1. Проверить наличие статических датасетов
ls benchmarks/data/static/
# Должно быть 10 файлов с расширением .json.txt

# 2. Запустить бенчмарки с готовыми датасетами
python benchmarks/run_benchmarks.py --tier 1 --dataset golden_set
python benchmarks/run_benchmarks.py --tier 2 --dataset golden_set

# 3. Просмотр результатов
ls benchmarks/reports/*.md
cat benchmarks/reports/retrieval_tier1_golden_set_*.md
```

**Преимущества:**
- ✅ Не требуется Docker
- ✅ Работает с пустой локальной БД
- ✅ Использует готовые статические датасеты
- ✅ Быстрый запуск
- ✅ Минимальное потребление ресурсов

**Ограничения:**
- ⚠️ Датасеты не обновляются автоматически
- ⚠️ Требует наличия статических файлов
- ⚠️ Без интеграции с QA-сервисом

### Режим 2: Полная интеграция (Docker + дамп БД)

**Оптимально для:**
- Тестирования с актуальными данными из продакшн
- Интеграционного тестирования
- Подготовки отчётов для публикации
- Мониторинга качества на реальных данных

**Пайплайн:**
```bash
cd Submodules/voproshalych

# 1. Подготовка дампа БД (опционально)
# Экспортировать дамп с серверной БД и положить в:
# benchmarks/data/dump/virtassist-main-YYYYMMDD-HHMMSS.sql.gz

# 2. Запуск проекта
docker compose up -d --build

# QA-сервис автоматически:
# - Создаст таблицы в PostgreSQL
# - Загрузит дампа БД (если есть)
# - Будет готов для бенчмарков

# 3. Генерация эмбеддингов (если в дампе их нет)
python benchmarks/generate_embeddings.py --all

# 4. Запуск бенчмарков
python benchmarks/run_benchmarks.py --tier 1 --dataset golden_set
python benchmarks/run_benchmarks.py --tier 2 --dataset golden_set

# 5. Загрузка нового дампа (опционально)
python benchmarks/load_database_dump.py --dump benchmarks/data/dump/new_dump.sql.gz
```

**Преимущества:**
- ✅ Свежие данные из продакшн БД
- ✅ Полная интеграция с QA-сервисом
- ✅ Автоматическая загрузка дампа
- ✅ Возможность регрессионного тестирования
- ✅ Реалистичные условия тестирования

**Ограничения:**
- ⚠️ Требуется Docker
- ⚠️ Время на развёртывание контейнеров
- ⚠️ Требует доступ к серверной БД для дампа
- ⚠️ Больше потребление ресурсов

### Сравнение режимов

| Параметр | Режим 1 (статические датасеты) | Режим 2 (Docker + дамп) |
|-----------|--------------------------------|-------------------------|
| Docker | ❌ Не требуется | ✅ Обязательно |
| Автозагрузка дампа | ❌ Нет | ✅ Да |
| Актуальность данных | ⚠️ Фиксированные | ✅ Свежие |
| Интеграция с QA-API | ❌ Нет | ✅ Да |
| Ресурсы | ✅ Минимальные | ⚠️ Требует больше |
| Время запуска | ✅ Мгновенно | ⚠️ ~5-10 минут |
| Сложность | ✅ Простая | ⚠️ Средняя |

### Рекомендации по выбору режима

**Используйте Режим 1, если:**
- Впервые запускаете бенчмарки
- Хотите проверить работоспособность кода
- Не нужен доступ к серверной БД
- Хотите сэкономить ресурсы
- Разрабатываете новые бенчарки

**Используйте Режим 2, если:**
- Нужны свежие данные из продакшн БД
- Хотите протестировать актуальную базу знаний
- Есть доступ к серверной БД для дампа
- Подготовливаете результаты для публикации
- Проводите регрессионное тестирование
- Интегрируете с CI/CD

## Просмотр дашборда

Интерактивный дашборд позволяет визуализировать метрики бенчмарков в удобном веб-интерфейсе.

### Требования

```bash
# Установить Gradio (опционально для дашборда)
pip install gradio
```

### Запуск дашборда

```bash
# Через специальный скрипт (рекомендуется)
python benchmarks/run_dashboard.py

# Или напрямую
python -m benchmarks.dashboard
```

Дашборд будет доступен по адресу: `http://localhost:7860`

### Функционал дашборда

**Раздел "Обзор":**
- 📊 Текущие значения метрик для всех бенчмарков
- 📋 Таблица со всеми метриками
- 🎯 Выбор бенчмарка и метрики для детального просмотра

**Раздел "История":**
- 📈 Графики изменения метрик во времени
- 📅 История по датам
- 📊 Сравнение разных запусков

**Раздел "Сравнение":**
- 🔍 Сравнение бенчарков между собой
- 📉 Визуализация различий
- ✅ Возможность выбора нескольких бенчарков

**Раздел "Автозагрузка дампа":**
- 🔄 Инструкция по загрузке дампа БД
- 📊 Проверка статистики таблиц
- 🗄️ Информация о покрытии эмбеддингами

### Подробная документация

Полная документация по дашборду находится в файле:
**`benchmarks/DASHBORAD.md`**

Содержание:
- Подробное описание всех компонентов
- Инструкция по настройке
- Примеры использования
- Возможности визуализации

## Полный рабочий цикл

### Режим 1 (статические датасеты)

```bash
# 1. Установка зависимостей
cd Submodules/voproshalych
uv sync

# 2. Проверка наличия статических датасетов
ls benchmarks/data/static/
# Ожидается 10 файлов .json.txt

# 3. Запуск бенчарков
# Tier 1
python benchmarks/run_benchmarks.py --tier 1 --dataset golden_set
python benchmarks/run_benchmarks.py --tier 1 --dataset low_quality
python benchmarks/run_benchmarks.py --tier 1 --dataset questions_with_url

# Tier 2
python benchmarks/run_benchmarks.py --tier 2 --dataset golden_set
python benchmarks/run_benchmarks.py --tier 2 --dataset questions_with_url
python benchmarks/run_benchmarks.py --tier 2 --dataset recent

# 4. Просмотр результатов
cat benchmarks/reports/retrieval_tier1_golden_set_*.md
cat benchmarks/reports/retrieval_tier2_golden_set_*.md

# 5. Запуск дашборда (опционально)
python benchmarks/run_dashboard.py
```

### Режим 2 (Docker + дамп БД)

```bash
# 1. Подготовка дампа (опционально)
# Сервер -> dump -> benchmarks/data/dump/

# 2. Запуск проекта
cd Submodules/voproshalych
docker compose up -d --build

# 3. Проверка статуса
docker compose ps
# Ожидается: qa (running), postgres (running)

# 4. Генерация эмбеддингов
python benchmarks/generate_embeddings.py --all

# 5. Проверка покрытия
python benchmarks/generate_embeddings.py --check-coverage

# 6. Запуск бенчарков
python benchmarks/run_benchmarks.py --tier 1 --dataset golden_set
python benchmarks/run_benchmarks.py --tier 2 --dataset golden_set

# 7. Загрузка нового дампа (опционально)
python benchmarks/load_database_dump.py \
    --dump benchmarks/data/dump/new_dump.sql.gz

# 8. Запуск дашборда
python benchmarks/run_dashboard.py
```

## Дополнительные инструменты

### Загрузка дампа БД

```bash
python benchmarks/load_database_dump.py --help

# Примеры
python benchmarks/load_database_dump.py \
    --dump benchmarks/data/dump/virtassist-main-20260213.sql.gz

python benchmarks/load_database_dump.py \
    --dump benchmarks/data/dump/dump.tar.gz \
    --format tar

# Проверка статистики после загрузки
python benchmarks/load_database_dump.py --stats-only
```

### Проверка покрытия эмбеддингами

```bash
python benchmarks/generate_embeddings.py --check-coverage

# Ожидаемый вывод:
# QA с эмбеддингами: 245/5135 (4.77%)
# Чанки с эмбеддингами: 0/15000 (0.00%)
```

## Конфигурация

### Переменные окружения

```bash
# База данных
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_HOST=localhost
POSTGRES_DB=voproshalych
POSTGRES_PORT=5432

# Модель эмбеддингов
EMBEDDING_MODEL_PATH=saved_models/multilingual-e5-large-wikiutmn
EMBEDDING_MODEL_NAME=intfloat/multilingual-e5-large

# Бенчмарки
BENCHMARK_TOP_K=10
BENCHMARK_OUTPUT_DIR=benchmarks/reports
BENCHMARK_TIMEOUT=300
```

### Файл конфигурации

Создайте `.env` файл в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` под ваши настройки.

## Логирование

Скрипты используют стандартный модуль `logging` с настроенным форматом:

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(levelname)s - %(message)s",
)
```

Уровни логирования:
- `DEBUG` — подробная отладочная информация
- `INFO` — информационные сообщения (по умолчанию)
- `WARNING` — предупреждения
- `ERROR` — ошибки
- `CRITICAL` — критические ошибки

## Отчёты

### Структура отчётов

Отчёты сохраняются в директории `benchmarks/reports/`:

**Markdown-отчёты:**
```
retrieval_tier{N}_{DATASET}_{TIMESTAMP}.md
```

**JSON-метрики:**
```
retrieval_tier{N}_{DATASET}_{TIMESTAMP}.json
```

**Примеры:**
```
retrieval_tier1_golden_set_20260213_150000.md
retrieval_tier1_golden_set_20260213_150000.json
retrieval_tier2_questions_with_url_20260213_151000.md
```

### Содержание отчёта

Каждый отчёт включает:
- 📊 Общая статистика (количество запросов, среднее время)
- 📈 Детальные метрики (recall@k, precision@k, MRR, NDCG)
- 🔍 Анализ покрытия эмбеддингами
- ✅ Примеры успешных поисков
- ❌ Примеры неудачных поисков
- 📋 Таблица с результатами по каждому запросу

## Архитектура

Подробная архитектура системы бенчарков описана в отдельном документе:

**Ссылка:** `../2026_benchmarks/architecture_snapshots/2026-02-13_Initial_Audit.md`

**Содержание:**
- Общая архитектура системы
- Поток данных при выполнении бенчарков
- Диаграммы компонентов и их взаимодействия
- Интеграция с QA-сервисом
- Критерии качества и метрики
- Технологический стек

## Зависимости

### Основные зависимости

```toml
[dependencies]
sentence-transformers = "^2.2.2"
sqlalchemy = "^2.0.0"
numpy = "^1.24.0"
pandas = "^2.0.0"
psycopg2-binary = "^2.9.0"
```

### Опциональные зависимости

```toml
[optional-dependencies]
dashboard = [
    "gradio>=4.0.0",
]
```

## Требования к окружению

**Минимальные требования:**
- Python 3.11+
- 4 GB RAM
- 10 GB дискового пространства

**Рекомендуемые требования:**
- Python 3.11+
- 8 GB RAM
- 20 GB дискового пространства
- GPU (для генерации эмбеддингов)

## Troubleshooting

### Проблема: Ошибка импорта модуля

**Решение:**
```bash
cd Submodules/voproshalych
uv sync
```

### Проблема: Нет доступа к базе данных

**Решение:**
```bash
# Проверьте контейнеры
docker compose ps

# Перезапустите postgres
docker compose restart postgres
```

### Проблема: Дашборд не запускается

**Решение:**
```bash
# Установите gradio
pip install gradio

# Или используйте статический режим
python benchmarks/run_benchmarks.py --tier 1 --dataset golden_set
cat benchmarks/reports/*.md
```

### Проблема: Низкое покрытие эмбеддингами

**Решение:**
```bash
# Генерируйте эмбеддинги для всех вопросов
python benchmarks/generate_embeddings.py --all --overwrite
```

## Планируемые улучшения

### В ближайшее время

- ⏳ Добавить метрики качества генерации (BERTScore, ROUGE)
- ⏳ Реализовать регрессионное тестирование
- ⏳ Добавить экспорт отчётов в PDF
- ⏳ Увеличить покрытие эмбеддингами до 80%+

### В долгосрочной перспективе

- ⏳ Интеграция с CI/CD
- ⏳ Мониторинг и алерты
- ⏳ A/B тестирование разных стратегий
- ⏳ Многоуровневые бенчарки
- ⏳ Добавление новых моделей эмбеддингов

## Полезные ссылки

- **Документация дашборда:** `benchmarks/DASHBORAD.md`
- **Архитектура системы:** `../2026_benchmarks/architecture_snapshots/`
- **Тесты:** `benchmarks/tests/`
- **Конфигурация:** `.env.example`

---

**Версия:** 2.0  
**Последнее обновление:** 2026-02-13  
**Статус:** ✅ Работоспособно
